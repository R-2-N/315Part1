void main() {
    var x = 5;    // x is statically typed, type is inferred
    	    	  // If a variable is initialized at when declared,
		  // its type is static.
    print(x);
    x = 5.1;
    print(x);
}
